import type React from "react"

import { Geist, Geist_Mono } from "next/font/google"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata = {
  title: "Flutter Basics - Complete Widget Guide",
  description: "Learn Flutter widgets with comprehensive examples, code snippets, and usage tips in English and Arabic",
  keywords: "Flutter, Widgets, Mobile Development, Dart, Tutorial",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
